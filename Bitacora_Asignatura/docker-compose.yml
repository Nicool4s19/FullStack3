services:
  mysql-bitacora :
    image: mysql:8.0
    container_name: mysql-bitacora
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: bitacora_db
    ports:
      - "3308:3306"
    volumes:
      - bitacora_data:/var/lib/mysql

volumes:
  bitacora_data: